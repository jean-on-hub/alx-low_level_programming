#include "main.h"

/**
 * _abs - function
 *
 * Description: computes absolute value
 * of an integer
 * @n: number to check
 *
 * Return: number
 */

int _abs(int n)
{
	return (n >= 0 ? n : n * -1);
}
